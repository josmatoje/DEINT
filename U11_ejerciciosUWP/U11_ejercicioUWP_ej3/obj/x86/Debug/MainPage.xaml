<Page
    x:Class="U11_ejercicioUWP_ej3.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:U11_ejercicioUWP_ej3"
    xmlns:vm="using:U11_ejerciciosUWP_ej3.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <vm:MainPageVM/>
    </Page.DataContext>

    <RelativePanel>
        <MenuBar x:ConnectionId='2' x:Name="barra" >
            <MenuBarItem Title="Persona">
                <MenuFlyoutItem Text="Eliminar" Command="{Binding Eliminador}">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Modifiers="Control" Key="Delete" />
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
            </MenuBarItem>
        </MenuBar>
        <TextBox x:ConnectionId='3' x:Name="buscador" Text="{Binding TextBoxBuscar, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" RelativePanel.Below="barra" PlaceholderText="Buscar" Width="200" Margin="20"/>
        <AppBarButton x:ConnectionId='4' x:Name="find" Icon="Find" Command="{Binding Buscador}" RelativePanel.RightOf="buscador" RelativePanel.AlignBottomWith="buscador"/>
        <ListView x:ConnectionId='5' x:Name="listado" ItemsSource="{Binding ListaPersonaOfrecido}" SelectedItem="{Binding PersonaSeleccionada, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectionMode="Single" IsItemClickEnabled="True" Margin="20" RelativePanel.Below="find">
            <ListView.ContextFlyout>
                <MenuFlyout>
                    <MenuFlyoutItem Text="Eliminar" Icon="Delete" Command="{Binding Eliminador}" />
                </MenuFlyout>
            </ListView.ContextFlyout>
            <ListView.ItemTemplate>
                <DataTemplate x:Name="CitaTemplate">
                    <RelativePanel MinWidth="350" Height="50" >
                        <TextBlock x:Name="nombre" Text="{Binding Nombre}" />
                        <TextBlock x:Name="apellido" Text="{Binding Apellido}" Margin="5,0" RelativePanel.RightOf="nombre" />
                    </RelativePanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel Orientation="Vertical" HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
        <AppBarButton x:ConnectionId='6' x:Name="delete" Icon="Delete" Command="{Binding Eliminador}" RelativePanel.Below="listado" RelativePanel.AlignLeftWith="find"/>

    </RelativePanel>
</Page>

