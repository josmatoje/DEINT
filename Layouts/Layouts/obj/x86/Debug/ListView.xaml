<Page
    x:Class="Layouts.ListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Layouts"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <RelativePanel>
        <StackPanel x:ConnectionId='2' x:Name="icons" Orientation="Horizontal">
            <AppBarButton Icon="Accept"/>
            <AppBarButton Icon="Delete"/>
            <AppBarButton Icon="Download"/>
            <AppBarButton Icon="Find"/>
        </StackPanel>
        <ListView x:ConnectionId='3' x:Name="lsvFernandos" RelativePanel.Below="icons" RelativePanel.AlignRightWith="icons" RelativePanel.AlignLeftWithPanel="True" 
                                    RelativePanel.Above="arrows" ScrollViewer.IsVerticalScrollChainingEnabled="True"
                                    Padding="5">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Nombre}" Margin="0,0,3,0" />
                        <TextBlock Text="{Binding Apellidos}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <AppBarButton x:ConnectionId='4' x:Name="add" Icon="Add" Label="Añadir" RelativePanel.AlignBottomWith="lsvFernandos" RelativePanel.AlignRightWith="lsvFernandos" 
                                    Margin="5"/>
        <StackPanel x:ConnectionId='5' x:Name="arrows" Orientation="Horizontal" HorizontalAlignment="Center" RelativePanel.AlignBottomWithPanel="True" 
                                    RelativePanel.AlignRightWith="icons" RelativePanel.AlignLeftWithPanel="True">
            <AppBarButton Icon="Back"/>
            <AppBarButton Icon="Forward"/>
        </StackPanel>
    </RelativePanel>
    
</Page>

